#include "main.h"
/**
 * _abs - a function that computes the absolute value
 * @n: the interger argument
 * Return: the absolute value of the interger argument
 */
int _abs(int n)
{
	if (n < 0)
		n = -n;
	return (n);
}
